# Chatbox

跨平台运行的 siri, 支持 macos 和 linux 上跑

首先安装: `brew install erchoc/tap/chatbox`

其他安装方式: 

- `npm i -g erchoc/chatbox`

- `curl https://chatbox.longye.site/install.sh | bash`


# 使用方法

### 前台模式
cb

# 守护进程部署
cb up

# 打开本地网页
cb open

# 配置语音和 LLM 模型
cb config

